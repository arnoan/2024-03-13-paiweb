# Instructions

```bash
# Clone the repo
git clone https://github.com/arnoan/2024-03-13-paiweb.git
cd 2024-03-13-paiweb

# Install dependencies
python -m venv venv --copies
pip install -r requirements.txt

# Copy .env.template file and add OpenAI API key
cp .env.template .env

# Run Jupyter lab
jupyter lab
```